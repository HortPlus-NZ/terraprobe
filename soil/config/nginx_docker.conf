# Default nginx config at /etc/nginx/nginx.conf

# the upstream component nginx needs to connect to
upstream soil {
    server app:8000;
}

server {
    listen 80;
    server_name localhost;  # Change this to your domain name in production

    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;

    client_max_body_size 100M;

    location / {
        proxy_pass http://soil;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /static/ {
        alias /app/static/;
    }

    location /media/ {
        alias /app/media/;
    }
}

# HTTPS server block (uncomment and configure when you have SSL certificates)
# server {
#     listen 443 ssl;
#     server_name localhost;  # Change this to your domain name in production
#
#     ssl_certificate /path/to/your/fullchain.pem;
#     ssl_certificate_key /path/to/your/privkey.pem;
#
#     # ... (rest of the configuration similar to the HTTP block)
# }